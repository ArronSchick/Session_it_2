<p id="notice"><%= notice %></p>

<h1>Bookeds</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @bookeds.each do |booked| %>
      <tr>
        <td><%= link_to 'Show', booked %></td>
        <td><%= link_to 'Edit', edit_booked_path(booked) %></td>
        <td><%= link_to 'Destroy', booked, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Booked', new_booked_path %>
