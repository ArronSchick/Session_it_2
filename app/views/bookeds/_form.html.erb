
<%= form_with(model: booked, url: booked_lesson_path(@lesson), local: true) do |form| %>
  <% if booked.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(booked.errors.count, "error") %> prohibited this booked from being saved:</h2>

      <ul>
        <% booked.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="actions btn btn-primary">
    <%= form.submit %>
  </div>
<% end %>
