<%= form_with(model: profile, local: true) do |form| %>
  <% if profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(profile.errors.count, "error") %> prohibited this profile from being saved:</h2>

      <ul>
        <% profile.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= form.label :picture %>
    <%= form.file_field :picture, accept: 'image/png, image/jpg, image/jpeg', :required => 'required'  %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, :required => 'required'  %>
  </div>

  <div class="field">
    <%= form.label :bio %>
    <%= form.text_field :bio, :required => 'required'  %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
