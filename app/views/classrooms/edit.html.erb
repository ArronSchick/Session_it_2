<h1>Editing Classroom</h1>


<table>
  <thead>
    <tr>
      <th>Edit Classroom</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
      <tr>
        <td><%= image_tag @classroom_view.picture, :class => "classroom_image" if @classroom_view.picture.attached? %></td>
        <td><%= @classroom_view.description %></td>
      </tr>
  </tbody>
</table>

<br>

<%= render 'form', classroom: @classroom_view %>

<%= link_to 'Back', classrooms_path, :class => "btn btn-secondary" %>